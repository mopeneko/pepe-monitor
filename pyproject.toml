[project]
name = "pepe-monitor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "hyperliquid-python-sdk>=0.15.0",
]

[dependency-groups]
dev = [
    "ruff>=0.11.11",
]
